# 2026-08-27

## 校准录入方向讨论（用户新倾向 → 已定调）
- 用户想把校准录入从「手动表单」升级为「上传资料→AI识别自动填写→AI提示必填项」的交互模式（增强互动感、消手动填摩擦）。
- 我认可方向，设三条红线：① AI 绝不脑补五维拆解（只抽 input+total+报价单明确写的锚）；② 锚不循环（纸价/工价须报价单白纸黑字，AI 不可反推）；③ 低置信须人工确认（qwen2.5vl:7b 视觉有限）。
- 现状：Ollama 未运行（localhost:11434 不通）、云端 key 未配，LLM 入口实质空、走规则回退。架构就绪（src/lib/llm/client.ts + 前端 localStorage AiSettings）。

## 用户最终定调（反馈后修正）
- **主做 Excel 上传**（大部分报价是 Excel），**图片作轻量辅助**，统一 AI 预填+确认。三条红线全认可。
- **职责边界澄清**：借助外部商业大模型把图/PDF 转成 md/excel 是**用户侧别的工具**完成，与本工具无关；本工具只接收转化好的结构化文件 + AI 预填 + 确认。
  → 红线在本工具内的落点收敛为两点：① 上传后 AI 预填绝不脑补五维（只填 input+total+文件明确写的锚）；② 确认环节把抽取结果+缺失/可疑全亮出由用户把关。不约束外部转换 prompt。
- Excel 宜"规则解析打底(SheetJS)+AI复核缺失/可疑"，比纯视觉更稳、零 Ollama 依赖；图片才更依赖视觉 LLM。

## 逻辑评估（用户问"逻辑怎么样"）
- 闭环成立，安全网是"确认环节"非 AI。
- 用户纠正（08:41）：外部工具是**忠实提取式**（只抽源文件已有字段、不补全五维），故"外部工具脑补五维污染"的缝隙不成立，无需确认环节勾选五维来源。
  → 红线在本工具内只需守"AI 预填不自行添加文件没有的五维"即可。
- 实现要点：Excel 规则打底+AI复核、图片才 AI 抽取；优先 Excel，md 次选(字段命名乱、抽取不稳)。
- 未落地（先讨论，不写代码）。动手前提=先确认 Ollama 可起或配云端 key。

## 实现落地（2026-08-27 下午，已 commit e3b50e1）
- 落地「上传识别式录入」：CalibrationUpload（Excel SheetJS 规则映射 / MD / 图片三入口）+ /api/calibration/extract（LLM 只抽原文、禁补全五维）+ CalibrationIntakeForm 支持 initial 预填 + 缺失必填提示。
- 模型：本地 Ollama 起服务并拉 qwen2.5:14b（文本抽取）+ qwen2.5vl:7b（视觉）；route 对 Ollama 自动映射本地模型名。
- 验证：tsc 0 错；Excel 映射 smoke 全字段命中；md 抽取实测正确（总价9200+纸价锚6200，无五维脑补）；image 路由分支走通。工作区干净、已提交未 push。
- AI 配置：前端 localStorage（AiSettings），Ollama 默认 baseUrl=localhost:11434；本机已起故 md/图片抽取现已可用。

## 一键启动脚本（2026-08-27 晚，用户诉求：不想每次找我拉 server）
- 新增 `start-dev.command`（macOS 双击运行）：cd 项目中文路径 → 移走旧 .next（mv 到 .next.bak-timestamp，规避缓存异常）→ export PATH 托管 Node22 → npm run dev。
- 已 chmod +x，bash -n 语法 OK，路径解析验证通过（node v22.22.2 / npm 10.9.7）。
- 用法：双击=终端开窗口跑 dev（保持窗口=运行，关窗口/Ctrl+C=停）。本机真实环境无沙箱门禁、无需 dangerouslyDisableSandbox。
- 注意：之前反复遇到的 safe-delete 门禁 / 沙箱限制是 WorkBuddy 工具运行环境特有，用户本机双击不受限，故脚本直接 npm run dev 即可。

## LM Studio 接入实测（2026-08-27 下午）
- 用户改用 LM Studio 本地加载 `qwen/qwen3.8-27b`（16.08 GB，4bit MLX，M4 24GB 可跑）。
- 启动路径：LM Studio → Local Server 页 → 点「Load Model」启动 API Server（默认端口 1234）。
- 工具配置：录入页「AI 模型配置」→ 选「本地 LM Studio」→ Base URL `http://localhost:1234`、API Key 留空、Model Name 填 API Model Identifier `qwen/qwen3.8-27b`。
- 修复问题：① 原测试连接 12s 超时会被 27B 首次响应 abort，已把 `pingModel`/`chatCompletion` 对本地端点超时延至 60s；② Qwen3.8 为 reasoning 模型，API 返回内容在 `reasoning_content` 字段，`content` 为空导致误判 LLM_EMPTY_RESPONSE，已兼容读取 `content ?? reasoning_content`。
- 验证：`tsc` 0 错；LM Studio `/v1/models` 探测模型 ID 正确；curl 直接调用 chat/completions 返回 reasoning_content。`tsx` 脚本实测 pingModel/chatCompletion 均通过。
- 模型限制：qwen3.8-27b 是文本模型（Capabilities 显示无 Vision），图片抽取仍需 Ollama 的 `qwen2.5vl:7b` 或商业多模态模型。
- 追加修复：Qwen3.8 默认会输出 `tool_calls`/`reasoning_content`，测试/正式调用需注入 system prompt 禁止工具调用并兼容 reasoning_content；`pingModel` max_tokens 16 过小导致 content 为空，已调到 64。

## 全局 AI 在线状态标志（2026-08-27 下午，已 commit）
- 用户诉求：无论在哪页都能随时看到 AI 是否 ready。
- 实现：新增 `src/components/analyze/GlobalAiStatus.tsx`（client 组件），挂 `src/app/layout.tsx` 根布局固定左下角常驻；展示 5 态（未配置/已关闭/检测中/在线绿/离线红）；每 60s 自动探测 + 窗口 focus/visibilitychange 即时复探；点击胶囊直接开「AI 模型配置」弹窗，关闭后自动重探。探测复用 `/api/ai-settings/test`。tsc 0 错、dev 主页/分析页 200。
- 注意：LM Studio 在本机是手动 Load Model 启动 API Server，所以状态会随用户开关模型在 online/offline 间切换，符合预期。

## 全局 AI 对话抽屉（2026-08-27 下午，已 commit）
- 用户想要 NotebookLM 式三栏（中间互动），但全站三栏重构改动过大，采用低成本全局抽屉：右下角紫色悬浮按钮 → 右侧滑入对话抽屉。
- 实现：新增 `src/components/analyze/AiChatDrawer.tsx` + 后端 `src/app/api/ai/chat/route.ts`（POST {messages, settings}，复用 chatCompletion，前端传 getAiSettings() 以支持本地 LM Studio）。多轮对话、Enter 发送、4 条引导语、未配置提示、抽屉内可开 AiSettingsModal。
- 实测：tsc 0 错；/api/ai/chat 接 qwen/qwen3.8-27b 正常返回中文；主页/分析页 200。
- 结论：用户想要的是"随时能对话的 AI 互动区"，不需要全站三栏化；抽屉方案已满足，且保持现有页面结构不动。

## AI 对话抽屉绑定「当前分析」为信息源（治乱说，2026-08-27 晚，已 commit 410a712）
- 用户指出全局 AI 无信息源会凭空编造（NotebookLM 核心是"选中信息源再发挥"）。已落地绑定：
- 新增 `src/lib/analyze-context.ts`（localStorage key `currentAnalyzeContext`）：`writeAnalyzeContext`/`readAnalyzeContext`/`clearAnalyzeContext`/`formatAnalyzeContext`。
- analyze 页 `useEffect([input, report])` 把当前 input + report（产品类型/数量/输入参数/总成本/维度拆分/优化建议/SQE 诊断）写入共享存储。
- 抽屉打开时读取并注入 system prompt 为【当前绑定信息源】，附加硬约束"只基于信息源回答，未提供的必须明说『资料中未提供』，不得编造"。顶部加信息源状态条（已绑定显示来源+解除/刷新；未绑定提示仅通用知识）。
- 实测：无信息源时模型正确回应"未提供报价单无法判断"，不编造；tsc 0 错；主页/分析页 200。
- 用户偏好确认：全局 AI 必须"基于选中信息源"，不接受裸调 LLM 乱说。下次若扩展 AI 互动，优先做信息源绑定/RAG，而非裸对话。

## P0 修复假绑定（2026-08-27 晚，已 commit f606358）
- review 指出绑定方案有假绑定风险：analyze 写 context 但离开不清除，在 VAVE/首页打开抽屉仍显示旧 analyze 绑定（误导）。
- 修复：① analyze 卸载 `clearAnalyzeContext()`；② 抽屉发送时实时读 `readAnalyzeContext()` 并 `setCtx` 同步状态条。离开 analyze 页后 context 失效、抽屉显示未绑定。tsc 0 错。
- 后续可选（用户选"只修 P0"，暂未做）：P1 VAVE 注册信息源、P2 统一活动信息源 store+多源可选+单配置入口（真 NotebookLM）、P3 RAG 检索。

## 真 NotebookLM 式 AI 工作台 `/ai`（2026-08-27 晚，本段收尾）
- 用户推翻"只修 P0"分步方案，明确要**真 NotebookLM 式**（选信息源→AI 基于它发挥），选「专用 AI 工作台页」而非全站三栏重构（全站重构改动大、回归面高、不符一期获客目标）。
- 实现：`src/app/ai/page.tsx` 三栏——左：可勾选信息源（当前分析 `readInfoSource()` + 全部已存 VAVE 项目 `listProjects()`，各 `formatReportContext` 预格式化）；中：对话（复用 `/api/ai/chat`，system 注入**选中源**+强制「引用句末标【来源：X】、未提供即明说、不编造」）；右：引用溯源（解析【来源：X】列出本次引用+已选中源）。
- 地基泛化：`src/lib/analyze-context.ts` → `src/lib/ai-context.ts`（`writeInfoSource`/`readInfoSource`/`clearInfoSource`/`formatReportContext`，带 `scope: "analyze"|"vave"` 为后续多源预留），analyze 页已改写；删旧文件。导航：首页+分析页头部加「AI 工作台」入口。
- 实测：tsc 0 错；`/ai`/首页/分析页 200；`/api/ai/chat` 双信息源测试模型正确返回「【来源：测试B】」。
- P1(给 VAVE 注册激活源给抽屉绑)因此被 `/ai` 多源取代，不再单独做；右下角悬浮抽屉保留作快捷入口（仅绑当前分析）。
- 后续增强（未做）：知识库/上传文档作为额外可勾选信息源；P3 RAG 检索。

## `/ai` 信息源扩为四类（2026-08-27 晚，本段收尾第二步，已 commit）
- 用户说"接着进行下一步"=把「知识库/上传文档」也接进 /ai 左栏（沿用 NotebookLM 选源思路，一步一步来）。
- 已实现 `src/app/ai/page.tsx` 左栏四类可勾选源：① 当前分析 ② 已存 VAVE 项目 ③ **成本知识库**(kind:kb，GET `/api/admin/knowledge-base` 过滤 material_price/process_rate/labor_rate/market_price 四类成本参数，`formatKbContext` 格式化；analysis_result/feedback 噪声不入源) ④ **上传文档**(kind:doc，左栏 file input 接 .txt/.md/.csv/.json，FileReader 读文本，存 state + localStorage `ai_uploaded_docs` 持久化、可删除；延续"图片/PDF 由用户外部转文本后上传"边界)。
- 四类源共用 `buildSystem` 的【信息源：X】注入 + 强制【来源：X】引用标注，右栏溯源同步。
- 实测：tsc 0 错；`/ai` 200；KB API 92 条（过滤后仅成本参数入源）。注：本地 27B reasoning 模型长多源提示偶发 OOM 被系统回收 exit137，属资源问题非代码缺陷，双源引用机制此前已验证。
- 下一步可选（未做）：P3 RAG 检索（大量文档时再上）；或把知识库/文档也接进右下角悬浮抽屉（目前抽屉仅绑当前分析）。

## `/ai` 工作台「项目绑定」主源模式（2026-08-27 晚，本段收尾第三步，已 commit）
- 用户确认：AI 工作台应按 NotebookLM「一个 notebook 绑定一组资料」运作，而非全局自由勾选。落地 `?bind` 入口分流：
  - `/ai?bind=analyze` 绑定当前分析（id=`current`）；`/ai?bind=vave:<projectId>` 绑定指定 VAVE 项目（id=项目 id）。主源**锁定不可取消**（左栏紫底+"主源"徽标+禁用 checkbox），并默认自动预挂知识库。
  - 首页「AI 工作台」按钮保持自由模式（不设 bind，沿用"当前分析优先/否则最近项目"）。
  - 按绑定键持久化对话历史：localStorage `ai_chat:<bindKey>`（自由=`ai_chat:free`），切 bind 加载对应历史（项目级留痕，契合 VAVE 谈判记录）；`loadedRef` 防加载回写覆盖。
  - 左栏绑定横幅（📌 已绑定主源 + 解除绑定跳 `/ai`）+ 提示"仍可在下方勾选其他源做对比"（主源锁定、附加源可增减，支持跨项目对比）。
- 入口三处：VAVE 选择页每项「就此项目问 AI」(`/ai?bind=vave:<id>`)；VAVE 激活态头部同链接（新增 `activeId` state 透传）；成本分析页结果区「就此分析问 AI」(`/ai?bind=analyze`)。
- 实测：tsc 0 错；`/ai`/`vave`/`analyze`/首页均 200。
- 用户偏好固化：AI 工作台 = 选中信息源再提问的 NotebookLM 模式；绑定/选源/溯源三位一体，不接受裸对话乱说。

## 目标 UI 架构定稿 + 两视角评审（2026-08-27 晚，设计讨论、未写代码）
- 用户拍板目标结构：顶层全局栏(品类只读 chip + 校准录入 + 知识库 + 设置 + AI 状态) + 三栏工作区(左项目导航/上传区、中工作页+底部AI对话、右AI结构化产出) + 底部进度条 + 前置载入页(工具介绍+本地模型预热)。
- 代码审计结论：agent 群(5 specialist + orchestrator + deriveAnalysisContext，纯函数) 100% 可复用，3 API 入口(/api/sessions、/api/vave/analyze、/api/batch/analyze)现成，VAVE 复用同一 runOrchestrator；不匹配仅在壳层(无全局 store、AI 入口三处并存、缺右栏 artifact schema、缺载入页/进度条)。
- 双视角评审 Backlog 已落 PROJECT_STATUS §10（优先级标注）：供应链高优=精度诚实标注+供应商报价拆解对比；UI 高优=AI入口收敛+中栏↔右栏联动+离线降级。两视角交汇最高价值项=供应商报价拆解对比工作台(新信息源类型+右栏五维偏差热力)。
- 落地顺序议定：L0 载入页 → P0 外壳骨架 → P1 左栏 → P2 中栏对话 → P3 右栏产出。用户尚未拍板"何时开工 L0"，待确认。

## 统一工作台 `/work` 全链路落地 + 旧页收敛（2026-08-27 深夜）
- 完成 P0–P3 统一工作台：`src/app/work/page.tsx` + `src/components/work/`（WorkbenchClient/LeftNav/AnalyzeWorkView/AiChatPanel/AiArtifactsPanel）。顶栏全局栏 + 三栏（左项目导航/上传、中工作页+底部AI对话、右结构化产出）+ 底部进度条。
- 复用现有成本引擎（UploadStep/InfoFormStep/ReportStep + /api/sessions）与 VAVE 工作台，未改计算逻辑；AI 对话复用 `/api/ai/chat`，回复后归纳 artifact 驱动右栏四卡。
- 首页主 CTA 改「进入工作台」/work；layout 移除全局 `AiChatDrawer`；`/analyze /vave /ai` 改为跳转页（显示"正在跳转到新工作台…"并 replace）。
- `GlobalAiStatus` 加 `usePathname` + mounted gate，在 `/work` `/intro` 隐藏，避免重复浮标。
- 验证：tsc 0 错；`/intro /work /analyze /vave /ai` 全 200；旧路由 HTML 不再含旧页核心文案；`/api/sessions` 生成五维报告正常；`/api/ai/chat` 未配置优雅降级。
- 已 commit `6d7aea0` + `384819e`，已 push GitHub（`lovecoolbear-create/PKG`）。本地 dev 在 3001 运行。

## fix: Hooks 顺序报错 + 入口未收敛（2026-08-27 深夜，用户实测反馈）
- 用户路径：首页 → `/intro` → 点击进入工作台 → 到了 `/analyze` 并触发 `GlobalAiStatus` "change in the order of Hooks" 报错。
- 根因：① `/intro` 按钮仍链 `/analyze`；② `GlobalAiStatus` 在 hook 声明前提前 `return null`，hydrate 后 hooks 数量变化。
- 修复：
  - `GlobalAiStatus` 把所有 hooks（usePathname/useState/useCallback/useEffect）移到顶部，条件 `return null` 放在所有 hooks 调用之后。
  - `/intro`「进入工作台」改 `/work`。
  - 首页 header/hero/品类卡片/CTA 所有入口统一 `/work`；品类卡片保留 `?product=` 参数。
  - `WorkbenchClient` 用 `useSearchParams` 读取 `?product=`，自动进入对应品类新建分析。
- 验证：tsc 0 错；首页无 `/analyze /vave /ai` 直链；`/work?product=flat_print` 200；旧路由跳转页不再触发 Hooks 报错。
- 已 commit `2eaa34b`，已 push GitHub。

## L0 载入页落地（2026-08-27 晚）
- 用户拍板"开工"，先做 L0（与新外壳解耦）：新增 `src/app/intro/page.tsx`（介绍+模型预热+进入，校正"六大维度"→"五维"）、`src/lib/ai-ready-store.ts`（全局 AI 就绪 store，无依赖）、`src/app/api/ai/warmup/route.ts`（LM Studio 触发加载+轮询就绪）、`client.ts` 导出 `getEndpointForLoad`、首页加「工具介绍」入口。
- 验证：tsc 0 错；`/intro` 200；warmup 三分支（disabled/未配置/本地端点未起）均正确降级。本地 dev 仍须先 `mv .next .next.bak-*` 再起（safe-delete 门禁拦批量删 .next）；curl 须 `--noproxy localhost` 绕过透明代理（HTTP_PROXY=127.0.0.1:62854 劫持 localhost 致 502）。
- 已 commit `c8a6513`（后随 `6d7aea0` 一起 push）。

## 死代码清理（2026-08-27 深夜，用户担心残留）
- 用户要求逐项正确清理残留在 /work 合并后的旧程序/页面，每步 tsc 验证。
- 删除零引用死文件：`src/components/analyze/AiChatDrawer.tsx`（layout 早已移除引用）、`src/components/vave/VaveNewForm.tsx`（仅被死跳转页引用，/work 新建 VAVE 走 onSaved 不调用）。
- 三个跳转壳页 `analyze/vave/ai` 的 Legacy 死代码块（`AnalyzeInnerLegacy`/`VavePageLegacy`/`AiWorkspacePageLegacy`）整块删除，仅留极简 redirect；旧页逻辑/死链接一并消失。
- 修复运行中页面旧直链：`/intro` 底部 VAVE/AI 链接 → `/work`；`/batch`「单笔分析」`/analyze` → `/work`；`ProjectListCard`「进入 VAVE 工作台」`/vave` → `/work`。
- 验证：全量 tsc 0 错；全仓 grep 无 `/analyze /vave /ai` 直链；`/work` 200、三跳转壳 200 且旧函数名残留为 0；`/intro /batch` 链接已改 /work。保留跳转壳路由（兼容旧书签）与仍被复用的 `VaveWorkbench`/`ProjectListCard`。
- 尚未 commit（待定 commit + push）。
